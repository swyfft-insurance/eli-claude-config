# Seeding

Use the `/seed` skill for interactive seeding. For manual seeding, see `Swyfft.Seeding/CLAUDE.md` for which script to use, what each does step-by-step, and when to clear `EFSeedingHistories`.

- Exit code 0 = seed completed. Trust it. Never re-run because you "didn't see all the logs."
