---
name: No logical-commits skill unless asked
description: Never invoke /logical-commits skill unless user explicitly requests it
type: feedback
---

Never use the `/logical-commits` skill unless the user explicitly asks for it. When the user says "lets commit" or similar, just commit everything directly — don't analyze and split into multiple commits unless specifically told to.
